import React from "react";
import { Text, StyleSheet } from "react-native";

const MyHistory = () => {
  return <Text style={styles.text}>MyHistory</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default MyHistory;
